# Optional: adds AI-written explanations of your results.
# Get a key at https://console.anthropic.com/settings/keys
# The app works fine without this — it falls back to built-in explanations.
ANTHROPIC_API_KEY=
